/**
 * [NextGalaxy global namespace]
 * @type {Object}
 */
window.NextGalaxy = window.NextGalaxy || {};

/**
 * [LOCATION constant that save the
 * current user location (without extension)
 * to further use at the moment to initialize
 * a new ui elemen]
 * @type {String}
 */
window.NextGalaxy.LOCATION = $('#main-form').data('location');


/**
 * [Utils namespace with function collection
 * that are useful to tackle some puntual cases]
 * @type {Object}
 */
window.NextGalaxy.Utils = {};